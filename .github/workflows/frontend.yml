name: Frontend

on: push

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - name: checkout code
        uses: actions/checkout@v5
      - name: Build
        run: |
          cd frontend
          npm ci
          npm run lint
  build:
    runs-on: ubuntu-latest
    steps:
      - name: checkout code
        uses: actions/checkout@v5
      - name: Build
        run: |
          cd frontend
          npm ci
          npm run build
